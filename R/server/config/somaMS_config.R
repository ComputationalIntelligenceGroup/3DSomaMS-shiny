#'
#'Constant variables
#'

infiles.accepted.types <- c("application/x-gzip", "application/zip", 
  "model/vrml", "x-world/x-vrml", 
  "model/ply", 
  "application/xml" , "text/xml",
  "text/plain")

infiles.zip.types <- c("application/x-gzip", "application/zip")